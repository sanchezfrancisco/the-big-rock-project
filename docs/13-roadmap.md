# Roadmap

Near-term:
- Metrics dashboard with p50/p95 aggregates and trend charts.
- Better semantic suggest pipeline (intent + file-level priors).
- Incremental indexing by file hash diff.

Mid-term:
- Language expansion beyond Python.
- Multi-repo routing and project profiles.
- Rich eval taxonomy (difficulty, query type, ambiguity).

Long-term:
- IDE extension with contextual ask/explain.
- Policy engine for outbound context filtering.
- Team mode with shared eval baselines.

